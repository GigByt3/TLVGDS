public class CharecterMananager : NetworkBehavior
{
    int[][] playerInformation = new int[][]
    for (int players = 0; players < 8; players++)
    {
        playerInformation[0][players] = 100
        CharecterIdentity id = new CharecterIdentity(players*100);
    }
}
